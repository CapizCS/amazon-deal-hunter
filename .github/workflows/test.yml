name: Deal Hunter Test

on:
  workflow_dispatch:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Scarica il progetto
        uses: actions/checkout@v4

      - name: Configura Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Test Telegram
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: |
          python - <<'PY'
          import os
          import urllib.request
          import urllib.parse

          token = os.environ["TELEGRAM_BOT_TOKEN"]

          # Per ora il chat ID lo inseriremo nel prossimo passaggio.
          print("Token Telegram trovato correttamente.")
          print("Test configurazione completato.")
          PY
